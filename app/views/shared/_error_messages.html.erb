<% if @user.errors.any? %>
  <div id="error_explanation">
    <div class="number-of-errors col-sm-4">
      Oh no! There were <%= pluralize(@user.errors.count, "error") %>.
    </div>
    <ul>
    <% @user.errors.full_messages.each_with_index do |msg, index| %>
      <li><%= index + 1 %>. <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
